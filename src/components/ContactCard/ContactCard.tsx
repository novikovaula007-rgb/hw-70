import {Grid} from "@mui/material";

const ContactCard = () => {
    return (
        <Grid container spacing={5}>
            <Grid size={4}>

            </Grid>
            <Grid size={8}>

            </Grid>
        </Grid>
    );
};

export default ContactCard;